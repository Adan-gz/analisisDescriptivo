% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generar_hojaExcel_descriptivos.R
\name{generar_hoja_excel_descriptivos}
\alias{generar_hoja_excel_descriptivos}
\title{Formatear descriptivos para hoja de Excel}
\usage{
generar_hoja_excel_descriptivos(
  tablas_descriptivos,
  concatenar = TRUE,
  añadir_titulos = TRUE,
  precision_num = 0.1,
  escala_num = 1,
  sufijo_num = "",
  prefijo_num = "",
  estilo_positivo_num = "none",
  precision_fac = 0.1,
  escala_fac = 100,
  sufijo_fac = "\%",
  estilo_positivo_fac = "none",
  separador_decimal = ",",
  separador_miles = "."
)
}
\arguments{
\item{tablas_descriptivos}{Lista nombrada con los descriptivos univariados, que debe contener los elementos \code{Numericas} y \code{Categoricas}.}

\item{precision_num}{Precisión para formatear los números. Por defecto es \code{0.1}.}

\item{escala_num}{Factor de escala para los números. Por defecto es \code{1}.}

\item{sufijo_num}{Sufijo a añadir a los números. Por defecto es cadena vacía.}

\item{prefijo_num}{Prefijo a añadir a los números. Por defecto es cadena vacía.}

\item{estilo_positivo_num}{Estilo para números positivos. Valores aceptados: \code{"none"}, \code{"plus"} o \code{"space"}. Por defecto es \code{"none"}.}

\item{precision_fac}{Precisión para formatear los porcentajes. Por defecto es \code{0.1}.}

\item{escala_fac}{Factor de escala para los porcentajes. Por defecto es \code{100}.}

\item{sufijo_fac}{Sufijo a añadir a los porcentajes. Por defecto es \code{"\%"}.}

\item{estilo_positivo_fac}{Estilo para porcentajes positivos. Valores aceptados: \code{"none"}, \code{"plus"} o \code{"space"}. Por defecto es \code{"none"}.}

\item{separador_decimal}{Carácter a utilizar como separador decimal. Por defecto es \code{","}.}

\item{separador_miles}{Carácter a utilizar como separador de miles. Por defecto es \code{"."}.}
}
\value{
Data frame listo para exportar a Excel, con los descriptivos formateados y concatenados.
}
\description{
Esta función formatea una lista de descriptivos univariados (generada con \code{generar_descriptivos_univariados})
para exportarla a una hoja de Excel. Aplica un formateo numérico a las variables numéricas y un formateo porcentual a
las variables categóricas, agregando además filas de encabezado a cada sección. Finalmente, concatena todas las tablas
en un único data frame.
}
\details{
La función realiza lo siguiente:
\enumerate{
\item Extrae los descriptivos numéricos y categóricos de la lista.
\item Aplica un formateo numérico a los valores numéricos utilizando \code{scales::number}, excepto en las columnas
\code{N} y \code{Missing}.
\item Aplica un formateo porcentual a los valores de variables categóricas utilizando \code{scales::percent}. En función
de si los datos están agrupados, se seleccionan diferentes columnas.
\item Agrega una fila de encabezado a cada sección (numérica y categórica).
\item Concatena todas las tablas utilizando la función \code{concatenar_tibbles}.
}
}
\examples{
\dontrun{
  # Suponiendo que 'mis_descriptivos' es la lista resultante de generar_descriptivos_univariados()
  hoja_excel <- generar_hoja_excel_descriptivos(
                  descriptivos_univariados = mis_descriptivos,
                  precision_num = 0.1,
                  escala_num = 1,
                  sufijo_num = "",
                  prefijo_num = "",
                  estilo_positivo_num = "none",
                  precision_fac = 0.1,
                  escala_fac = 100,
                  sufijo_fac = "\%",
                  estilo_positivo_fac = "none",
                  separador_decimal = ",",
                  separador_miles = "."
                )
}

}
