% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crear_Excel.R
\name{crear_Excel}
\alias{crear_Excel}
\title{Genera un workbook Excel con múltiples hojas, cada una con tablas y títulos formateados}
\usage{
crear_Excel(
  list_list_tablas,
  titulos_principales = NULL,
  titulos_tablas = NULL,
  nombres_hojas = NULL,
  estilo_tabla = "TableStyleLight1",
  tabla_conFiltro = TRUE,
  fuente = "Calibri",
  titulo_principal_tamanyo = 14,
  titulo_principal_colorFuente = "#00b2a9",
  titulo_tabla_colorFuente = "#00b2a9",
  titulo_tabla_tamanyo = 12,
  nombre_archivo = NULL,
  exportar = FALSE,
  sobreescribir_archivo = TRUE,
  formatear = T
)
}
\arguments{
\item{list_list_tablas}{Lista de listas; cada sublista contiene los data.frames a exportar en una hoja.}

\item{titulos_principales}{Vector de títulos principales para cada hoja. Si es \code{NULL} se asignan valores por defecto.}

\item{titulos_tablas}{Lista de vectores, donde cada vector contiene los títulos de las tablas correspondientes a cada hoja.}

\item{nombres_hojas}{Vector con los nombres de las hojas. Si es \code{NULL} se asignan nombres por defecto.}

\item{estilo_tabla}{Nombre del estilo de tabla predefinido de Excel (por ejemplo, "TableStyleLight1").}

\item{tabla_conFiltro}{Lógico; si es \code{TRUE} las tablas se crean con filtros.}

\item{fuente}{Fuente a utilizar para los títulos.}

\item{titulo_principal_tamanyo}{Tamaño de fuente para el título principal.}

\item{titulo_principal_colorFuente}{Color de la fuente para el título principal.}

\item{titulo_tabla_colorFuente}{Color de la fuente para los títulos de las tablas.}

\item{titulo_tabla_tamanyo}{Tamaño de fuente para los títulos de las tablas.}

\item{nombre_archivo}{Nombre base del archivo Excel a exportar (sin extensión). Si es \code{NULL} se asigna un nombre por defecto.}

\item{exportar}{Lógico; si es \code{TRUE} guarda el archivo Excel.}

\item{sobreescribir_archivo}{Lógico; si es \code{TRUE} sobrescribe el archivo si ya existe.}

\item{formatear}{Cuando el input \code{list_list_tablas} proviene de utilizar \code{generar_descriptivos_agrupados}
se formatea los porcentajes y los números. Por defecto \code{TRUE}. Este parámetro está pendiente de mejoras.}
}
\value{
Un objeto \code{workbook} de \code{openxlsx} con todas las hojas y tablas agregadas.
}
\description{
Esta función crea un workbook Excel en el que cada hoja contiene un título principal,
títulos para cada tabla y las tablas formateadas. Para cada hoja se utiliza la función \code{generar_hojaExcel}.
}
\examples{

library(dplyr)
temp <- mtcars \%>\%
 mutate('cyl'=as.character(cyl),
        'carb'=factor(carb),
            'gear' = factor(gear),
            'w'=rlnorm(32))
temp_biv_simple <- generar_descriptivos_agrupados(
 datos = temp,
 vars_grupo = "cyl",
 vars_numericas = "mpg",
 vars_categoricas = "vs",
 var_peso = "w"
 )

crear_Excel(temp_biv_simple, exportar = T, formatear = T)
#'
## Otro ejemplo
list_temp <- list(head(mtcars,5),head(mtcars,5),head(mtcars,5))
list_list_temp <- list(list_temp,list_temp)

workoob_temp <- crear_Excel(
 list_list_temp, # lista de listas
 exportar = T
 )

}
