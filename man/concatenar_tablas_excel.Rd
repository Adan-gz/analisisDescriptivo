% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concatenar_tablas_excel.R
\name{concatenar_tablas_excel}
\alias{concatenar_tablas_excel}
\title{Concatenar múltiples tablas para exportación a Excel (deprecated)}
\usage{
concatenar_tablas_excel(
  lista_tablas,
  n_filas_vacias = 2,
  ajustar = TRUE,
  ajustar_proporciones = NULL,
  precision = 0.1
)
}
\arguments{
\item{lista_tablas}{Lista de data frames o tibbles a concatenar. Si se proporciona un único data frame,
se convertirá en una lista de un elemento.}

\item{n_filas_vacias}{Número de filas vacías que se insertarán entre cada tabla. Por defecto es \code{2}.}

\item{ajustar}{Lógico. Si es \code{TRUE} (por defecto), se aplicará \code{preparar_tabla_excel} a cada tabla.}

\item{ajustar_proporciones}{Vector de caracteres con los nombres de las columnas a formatear como proporciones.}

\item{precision}{Precisión para el formateo de las proporciones. Por defecto es \code{0.1}.}
}
\value{
Data frame resultante de concatenar todas las tablas, listo para exportación a Excel.
}
\description{
Une varias tablas (tibbles o data frames) en una sola, separándolas mediante filas vacías.
Esto permite exportar muchas tablas en una única hoja de Excel. Opcionalmente, se puede formatear cada
tabla individualmente usando \code{\link{preparar_tabla_excel}}.
}
\details{
La función itera sobre cada tabla en \code{lista_tablas} y, si \code{ajustar} es \code{TRUE},
formatea la tabla mediante \code{preparar_tabla_excel}. Luego, añade filas vacías (según \code{n_filas_vacias})
para separar visualmente cada tabla. Finalmente, concatena todas las tablas en un único data frame.
}
\note{
\strong{Deprecated}  Esta función está obsoleta y será eliminada en futuras actualizaciones. Utiliza las funciones \verb{crear_Excel_*()} en su lugar.
}
\examples{
\dontrun{
  tabla1 <- tibble(a = 1:3, b = 4:6)
  tabla2 <- tibble(a = 7:9, b = 10:12)
  resultado <- concatenar_tablas_excel(list(tabla1, tabla2), n_filas_vacias = 2, ajustar = TRUE)
}
}
